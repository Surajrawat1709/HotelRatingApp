server:
  port: 8086

spring:
  application:
    name: CONFIG-SERVER

  cloud:
    config:
      server:
        git:
          uri: https://github.com/Surajrawat1709/microservices-tutorial-config
          clone-on-start: true